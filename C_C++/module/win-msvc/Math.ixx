export module Math;
import std;

export int Add(int, int);
//export auto Add(auto, auto);

//export template<typename T>
//T add(T a, T b);

export auto Print(std::string msg) -> void; 
//export auto Print(auto&& msg) -> void;